# My custom alias
alias -g srv="python manage.py runserver"
 
# alias -g attach="tmux attach"
alias -g reload="source ~/.zshrc"
alias gpom='git push origin master'
alias home='cd ~'

#make gvim launch on terminal
alias -g gvim='gvim -v'
# Launching tmuxinator as mux
alias -g mux='tmuxinator'

#Git CLI alias"
alias g='git'
alias gi='git init'
alias ga='git add'
alias gra='git remote add'
alias gs='git status'
alias migrate='python manage.py migrate'
alias makemigrations='python manage.py makemigrations'
alias srm='sudo rm -r '
