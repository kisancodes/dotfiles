# ~/.tmuxinator/djreact.yml

name: djreact
root: ~/Projects/subisu

windows:
  - editor:
      layout: main-horizontal
      panes:
        - vim
        - cd backend; pipenv shell
        - cd frontend
  - console:
      panes:
        - docker-compose up
